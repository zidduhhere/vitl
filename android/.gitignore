# Android build outputs
.gradle/
build/
app/build/

# Local machine config
local.properties

# Android Studio project files
*.iml
.idea/

# The AAR is generated by mobile/build_android.sh — not committed.
# Commit it only if you want a "no-build-step" setup for CI.
app/libs/vitallink.aar
